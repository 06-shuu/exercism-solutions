#if !defined(TRINARY_H)
#define TRINARY_H
#include <iostream>
#include <cmath>
#include <string>

namespace trinary {

	int to_decimal(std::string trinary);
}  // namespace trinary

#endif // TRINARY_H